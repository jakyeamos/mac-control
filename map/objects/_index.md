# mac-control object index

All entries are `status: stub` until a human-reviewed citation pass.

- `guarded-invocation` — guarded command routing and execution in `Sources/`;
  `universe: live`; does not imply permission to bypass owner gates.
- `focus-display-sessions` — focus/display/session surfaces in `Sources/` and
  related contracts; `universe: unknown`; native/CLI grouping remains open.
- `diagnostics-provider-evidence` — diagnostics, provider adapters, fixtures,
  and evidence docs; `universe: live`; provider evidence does not prove
  visible behavior by itself.
- `distribution-agent-contract` — `skills/`, docs, scripts, packaging, and
  agent-facing surfaces; `universe: live`; does not own runtime authorization.
- `support-and-tests` — `Tests/`, fixtures, and benchmarks; `universe: live`;
  does not automatically become a child Compass.

